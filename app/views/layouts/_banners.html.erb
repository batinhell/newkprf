<% @banners = Banner.all %>
<% @banners.each do |banner| %>
  <div class="banner">
    <%= link_to(image_tag(banner.image.url), banner.url, :target => "_blank") %>
  </div>
<% end %>
<div class="clearfix"></div>
