<div class="photos">
  <h2><%= @album.name %></h2>
  <p><%= @album.description %></p>
  <% @photos.each do |photo| %>
    <a href="<%= photo.image.url %>" rel="lightbox">
      <img src="<%= photo.image.thumb.url %>">
    </a>
  <% end %>
</div>

