<%= form_for(@agit) do |f| %>
  <% if @agit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@agit.errors.count, "error") %> prohibited this agit from being saved:</h2>

      <ul>
      <% @agit.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :image %><br />
    <%= f.text_field :image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
